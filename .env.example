# Optional sync host override for split-host development, for example: localhost:8787
VITE_SYNC_HOST=

# Optional party name that maps to the Durable Object route (defaults to kniffel-sync)
VITE_SYNC_PARTY=kniffel-sync
